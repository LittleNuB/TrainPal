# 连续教学分块：追加两次真实复评

结论：**未通过真实质量门槛，不能据此宣布去重问题解决。** #32 及依赖批次继续保持 Draft，不合并或部署。

## 授权与方法

- 用户在 #32 后明确批准 eval-07、eval-08 各一次完整收费分析。本轮已完成 2/2，未重试、未追加第三次；此前 12 次记录不覆盖、不重新计数。
- 实测代码 `6113aab`，功能提交 `6f99ca2`，融合提示词 2.3.0；沿用 Ark/ASR 和开发预算 90/590/600 秒，不切换 Provider，不改变 300 秒上限。
- 使用实际完整分析管线，不是缓存结果或假 Provider；从分析开始到返回计时，包含媒体处理和一次极短的内存离线对照，不含浏览器上传或用户操作。
- 参考仍为已冻结的 H/A/U 记录。两条样本的动作名称、范围和参数主要是 A（AI 初标/补核），并非新增人工金标准；热身/拉伸细分仍有 U。本轮不是盲测。
- 额外对照复用**同一份上游证据及同一次真实模型分组**，在内存执行 `6a93e64` 的严格共同交集规则，不再次调用 Provider。它隔离的是代码时间护栏差异，不是重新跑旧提示词的 A/B 实验。
- 仅最终结构化产品结果和非内容诊断计数留在本机忽略目录；完整转写、原始模型响应、分组原文及临时媒体不进入公开材料。两个运行均核验本地临时目录清空，不代表供应商侧保留政策验证。

## 本轮结果

| 样本 | 时长 / 分析用时 | 候选 / 待确认 | 要点 | 主要结果 |
|---|---|---|---|---|
| eval-07 | 67.83 / 37.88 秒 | 6 / 2 | 7，均带语音出处 | 四项力量动作各一项，未见重复；弹力带和泡沫轴仅留一条概括性语音热身，细分视觉依据不足 |
| eval-08 | 207.77 / 84.58 秒 | 14 / 13 | 3，均带视觉出处 | 推肩拆成 5 项、侧平举 3 项、杠铃提拉 1 项、反向飞鸟 3 项，另有热身和拉伸各 1 项 |

两次均返回 complete、覆盖缺口 0；这仅表示分析覆盖，不表示语义完整或正确。所有返回时间范围均在来源时长内，单动作要点均不超过三条，两次未出现英文字母动作名；不据此宣称命名已稳定。

### eval-07

四个主要力量动作与冻结参考可对应：高位下拉、坐姿划船、单臂器械划船、大剪刀下拉；四项均保留 4 组、8–12 次，默认范围约为 15–27 / 27–37 / 37–47 / 47–57 秒。可选爬坡有氧保留 1200 秒，仍为语音单源待确认。

两种热身不再以两个独立视觉内容保留，只得到 11.68–14.64 秒的一条概括语音项。虽然名称包含两种内容，也不能当作细分动作完整保留或计为“7/7 全部正确”。本次没有主要力量动作误合并，但热身粒度损失需要继续核验，不能把候选数从旧轮次的 8/10/12 降到 6 当作纯去重收益。

### eval-08

四项主训练都能找到对应，然而仅这四项共形成 12 行，相对四项独立训练多出 8 行；其中包含准备、姿势讲解被拆为独立动作的问题，不把所有多余行都称为完全相同的重复。此前单次为 10 项/8 待确认，本次 14 项/13 待确认，质量没有达到预期。

推肩有语音主项及准备/握法、姿势调整、教学分块、向上推起等独立条目；侧平举和反向飞鸟也各保留三项。反向飞鸟既有 150–160 秒短视觉段，也有 150–200.5 秒长视觉段及 150.57–200.35 秒语音段，但仍未合为一个动作，因此 #31 的组内默认范围选择修复不能替未通过身份校验的条目解决重复。

语音主项/后段视觉仍有 4×10，部分早段视觉则是 8–10 次。这些数值没有静默丢弃，但仍散在多项里，最终 `parameter_conflicts` 为空，没有达到“同次动作一个待确认项、显式展示参数冲突”的预期。热身 2×15 和拉伸 2×20 秒的分项适用范围仍按 U 处理。三条要点中一条仅泛称练后拉伸，不能将结构有出处视作提醒内容已合格。

## 同输入规则对照

| 样本 | 模型提出分组数 | 旧规则候选 / 待确认 | 新规则候选 / 待确认 | 连续教学例外触发组数 |
|---|---:|---:|---:|---:|
| eval-07 | 8 | 6 / 2 | 6 / 2 | 0 |
| eval-08 | 6 | 14 / 13 | 14 / 13 | 0 |

两次语义阶段各只有一次真实模型调用。分组总数包含内容角色裁决，不能把“6 个分组”表述成模型正确识别了 6 个训练动作。eval-08 的分组经校验拆回更多候选，说明仍有分组被代码拒绝；两份结果均未触发本次新增例外。

因此可以确认：**本次两条真实输入未展示新增时间例外的收益；同输入下新增例外也没有改变候选/待确认数量。** 不能将跨运行变化单独归因于代码，也不能据此证明新提示词无影响、规则永远无效或没有任何误合并风险。

未持久化原始分组、上游角色和编号，本轮只有汇总诊断，尚不能精确断言每个失败分组是被时间、编号、内容角色还是组外视觉边界拒绝。不得凭最终名称猜一个原因就改护栏。

## 后续处理与门槛

1. 先为融合拒绝增加**非内容原因计数**及合成公共接口回归，区分时间不连通、未知/跟练角色、编号冲突、组外视觉边界和模型不确定；不保存模型原文，不再用单个 `semantic_fusion_conflict` 混淆身份与参数问题。
2. 以明确失败原因为依据设计最小修复。现阶段不批准自动放宽端点接触、编号、真实再次执行或变式保护，也不增加 Agent 或更换模型。
3. 两次追加预算已用完；若需真实输入的更细诊断，先申请额外运行，不自动再跑。不以合成回归、耗时单次下降或低待确认数量作为质量通过。
4. 保留 #6 开放，#32 保持 Draft；小米自带浏览器真人语音、主分支合并和最终干净环境验收仍未完成。
