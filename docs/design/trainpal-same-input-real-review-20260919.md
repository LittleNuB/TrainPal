# 同输入真实复测：编号修复有效，提醒归属仍未通过

## 范围与结论

用户单独授权最多四次完整分析：eval-08 先跑一次，通过后才跑 eval-07/09/10 各一次；任何失败即停止，不自动重试。本轮实际只运行 eval-08 一次，另外三次未执行，已写入本地停止标记。

运行代码 `d6b0d3597c11c57144c97dbb306372e3b60bd6e6`，依赖 Draft #34 的编号格式修复。Provider、提示词、300 秒上限和接受规则未改。使用既有本地开发超时配置 90/590/600 秒，不是生产默认性能测试。

**结论：去重和冲突呈现有明确改善；第一道质量门槛仍未通过。** 四个主体动作各有一个条目，但动作交界处存在一条提醒来源错配，因此不继续后三次，也不宣布分析或整体迭代验收完成。

## 同一输入、同一模型分组的受控对照

本次源时长 207.77 秒，完整运行耗时 85.97 秒；覆盖 complete，缺口 0，临时媒体清理通过。一次逻辑语义模型分组提出 6 组；旧规则离线回放没有第二次 Provider 请求。

| 指标 | 当前代码 + a294f0f 旧编号策略 | 当前编号策略 |
| --- | ---: | ---: |
| 最终候选 | 12 | 6 |
| 待确认候选 | 11 | 4 |
| 单动作参数冲突结构 | 0 | 2 |
| 正式分组拒绝 | 4 | 1 |
| 编号身份冲突 | 3 | 0 |
| 共同时间交集通过 | 1 | 4 |

固定分类记录 `format_equivalence_resolved=3`，没有记录其他标签冲突分类。当前余下的正式分组拒绝伴随模型不确定和内容角色原因；另有一次参考关联拒绝，伴随方向、参数冲突。原因计数可以重叠，不能相加当成失败组数。

本对照的唯一策略差异是完整动作序号写法的兼容，证实本次输入中存在该格式误拦，修复后消除了这三处编号冲突。它不是完整旧构建 A/B，也不能倒推前一次未保留标签的运行必然具有相同原因。一次运行不能证明输出稳定性、总体准确率或 P95。

## 冻结参考复核

继续使用 [H/A/U 冻结参考](trainpal-release-reference-freeze-20260919.md)。eval-08 的动作名称、时间与参数参考属于 A（AI 初标/补核），不是用户逐项确认的真值。

- 四项主体动作分别为坐姿哑铃推肩、哑铃侧平举、杠铃提拉、反向蝴蝶机；本次各保留一个条目，未见同次主体示范的重复执行项。教学准备没有单独增加执行条目。
- 其余两个条目为热身和拉伸，均待确认；细分与参数适用范围继续按 U 单列，不纳入确定性动作匹配率。
- 推肩与侧平举各保留一个待确认条目，次数冲突分别保留两种数值及各自出处，未静默选值。其余两项主体动作不待确认。这解释了当前 4 项待确认，而非通过降低确认条件减少数量。
- 默认回看仍选自最终结果中的原始视觉范围，备选范围与语音/视觉来源条目仍保留。此项只核对结果结构与大段参考对应，不代表每一个起止点均已逐帧确认。
- 输出共 12 条要点（4 条语音、8 条视觉）。本轮发现下述明确错配后停止扩展；其余要点未全部完成逐条语义核验，不能把结构合法记为提醒正确。

## 阻塞项：动作交界处的提醒归属

反向飞鸟条目含一条组数收尾提示，标注视觉出处为 150–152 秒。对授权原视频 148–154 秒附近进行本地逐秒抽帧复核，可见该提示出现在上一项杠铃提拉结尾，随后才切到反向飞鸟介绍。当前结果却把它作为反向飞鸟的来源化要点。

这是**提醒与动作的来源绑定错误**，即使两个动作恰好都有四组，也不能据此视为来源正确。该条目的默认范围还覆盖了这处交界，进一步说明“时间在候选范围内”不足以证明语义归属。模型输出名称另有器械字词误写，作为次要质量问题记录。

本轮未持久化上游原始证据或模型分组，不能从计数进一步断言是哪一层首次产生该错配，也不能用只检查时间范围的规则宣称已修复。后续应先用合成的相邻动作/收尾提示案例复现，再检查提取、融合和要点选择的归属约束；不得盲目剔除所有参数类提示，或只扩大/缩小时间窗掩盖问题。修复另作小批实现与审查，本轮不改规则。

## 停止、隐私与交付

- 本轮收费完整运行已用 1/4；剩余三条因第一道门槛失败而未执行。无自动重跑、无额外 ASR/模型核验调用。
- 本地保留最终产品候选与固定计数，不保留原始转写、标签、分组成员或模型原文。用于人工查看的临时抽帧已删除；原视频未修改，公开材料不包含媒体、截图、身份或本机路径。
- 本文是运行后报告；[准备记录](trainpal-same-input-retest-readiness-20260919.md)中的未调用说明是授权前的历史状态。
- Draft #35 继续依赖 #34；未合并、部署或开放网络，未修改旧工作树、标注和现有预览。
- 当前代码此前完整检查为前端 275、后端 963、E2E 49 项通过；本次仅补报告，不能用自动化通过替代本次真实质量失败。实际结果的完整产品旅程、小米自带浏览器真人语音及干净主分支验收仍未完成。
