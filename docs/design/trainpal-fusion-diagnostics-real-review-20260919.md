# 融合诊断：一次追加真实复评

结论：**本次重复候选的主要拒绝原因是编号身份冲突，不是时间校验；真实质量仍未通过。** 仅描述本次运行，不倒推此前未记录诊断的运行。

## 授权与方法

用户在 #33 后明确允许对 eval-08 追加一次完整收费分析。已完成 1/1，未重跑其他视频、未重试整次分析。使用代码 `63aecf99093a0bbba48866993e93843dc0055ff7`，现有 Ark/ASR Provider 与文本/视觉模型配置未变，融合提示词 2.3.0，开发预算 90/590/600 秒，300 秒上限不变。

使用真实完整分析管线；记录正式融合结果的固定诊断计数，模型分组仅在内存中计数，不落盘。耗时从管线启动计至返回，含本地媒体处理，不含浏览器上传和用户操作。参考沿用冻结 H/A/U 记录；本视频的动作及参数主要为 A，热身/拉伸细分仍有 U，不是盲测或人工金标准。

## 结果

| 项目 | 实测 |
|---|---|
| 视频时长 / 分析用时 | 207.77 / 89.56 秒 |
| 融合模型调用 / 融合阶段用时 | 1 次 / 2.17 秒 |
| 模型分组 / 最终候选 / 待确认 | 6 / 13 / 12 |
| 覆盖 / 缺口 | complete / 0，仅说明处理覆盖 |
| 参数冲突结构 | 0，冲突数值仍散在不同候选，不代表无冲突 |
| 动作要点 | 22 条：语音出处 6、视觉出处 16；每项最多 3 条 |
| 时间结构 / 命名 | 范围均合法；未见英文字母动作名 |
| 临时媒体清理 | 本地临时目录为空；不代表供应商侧留存验证 |

四项主要力量动作仍能对应，但推肩 4 行、侧平举 3 行、杠铃提拉 1 行、反向飞鸟 3 行，共 11 行；另有热身和拉伸各 1 行。教学准备与过程仍形成多项。开头三联画热身只保留一个具体动作，不能据此认为热身完整识别。

推肩与侧平举仍有 8–10 次和 4×10 的来源值散落在不同候选，未实现同动作单项展示冲突。22 条要点只核验数量、来源类别和时间结构，未逐条回看原视频核实语义；不能称为 22 条正确提醒。与上次 14 项相比减少一项、要点增加，均不能归因于本次诊断代码或视作质量提升。

## 拒绝原因

| 固定诊断码 | 计数 | 含义 |
|---|---:|---|
| `group_rejected` | 3 | 正式动作分组合并被拒绝 |
| `sequence_label_conflict` | 3 | 三个被拒分组均存在归一后的编号身份冲突 |
| `common_intersection_accepted` | 2 | 两个正式分组通过共同交集路径；不等于两次去重 |
| `reference_rejected` | 1 | 一个预告/回顾来源关联被拒绝 |
| `reference_uncertain` | 1 | 该参考关联本身不确定 |
| `reference_target_unresolved` | 1 | 目标正式动作未成功解析 |
| `reference_parameter_conflict` | 1 | 参考与目标存在参数冲突 |
| `reference_label_conflict` | 1 | 参考与目标存在编号冲突 |
| `reference_direction_conflict` | 1 | 参考方向与预告/回顾要求不符 |

未出现 `time_alignment_rejected` 或任何连续教学拒绝原因，也未触发 `continuous_teaching_accepted`。本次三个正式分组只有编号拒绝原因；一个参考分组同时命中五种原因，不能计成五个失败分组。不能凭汇总计数给具体候选逐一绑定分组身份。

## 解释与下一步

已定位到**拒绝发生在哪条规则**，但尚未确定**为何编号发生冲突**。原始标签、分组成员及视觉描述没有保存，不能断言一定是模型乱编号、同义编号格式，或把真正不同动作错分在同组。

只读检查发现，当前身份归一仅接受完整数字、零至十的单字中文数字，以及可选 `动作` / `#` 前缀；其他格式保持原字符串。视觉合同要求读取真实可见编号，却未定义跨分块的规范化表示。因此“同一编号的格式变化”与“跨块错误编号”都是待验证假设，不是已确认根因。

下一批应先用合成样本覆盖编号格式等价、真实不同编号、左右侧/轮次，以及模型误分组；再决定是否收紧上游编号合同或补充无歧义格式归一。不得删除编号保护、只抽字符串中的数字、让语义模型覆盖明确冲突，或继续扩大时间例外。若需真实标签模式诊断，应先定义不含原文的分类计数，再另行申请调用；本次预算已用完。

#6 保持开放，#33 保持 Draft。本次没有改运行代码、合并、部署、开放网络或替换演示服务；小米真人语音与整体交付门槛仍未完成。
