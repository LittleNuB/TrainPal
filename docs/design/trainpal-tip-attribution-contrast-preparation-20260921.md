# 要点归属对照：离线准备记录

日期：2026-09-21。规格：[最小对照](trainpal-tip-attribution-contrast-spec-20260921.md)。
本轮没有真实 Provider 调用；下面不是模型质量结果。

## 已准备

- 实验工具：`services/analysis-api/scripts/tip_attribution_contrast.py`，不由应用导入，不搜索凭据、不读取本地环境配置、不提供自动联网命令。
- 固定构造案例：同目录 `fixtures/tip_attribution_contrast.json`。两例的视觉输入已逐字段核对，与前轮被测输入相同。
- 通过既有 Provider → 候选融合接口准备三种方式。A 使用原生产提示词和输入；B 无损展开要点；C 在 B 基础上给定正确动作分组，但不提供要点答案。输出 schema、生产解析和最终过滤保持一致。
- 离线测试：`uv run --project services/analysis-api pytest services/analysis-api/tests/test_tip_attribution_contrast.py`。HTTP 外部边界替身不产生收费请求。
- 测试先复现工具缺失、对照方式缺失，以及 C 擅自改名却被判通过的缺口，再完成实现；后者现在报告 `fixed_group_drift`，不会在修正模型结果后冒充通过。

## 冻结的输入指纹

以下六格用离线 HTTP 替身走实际请求构建得到。请求指纹是规范化后的提示词与观察输入 SHA-256，不是整个 HTTP 请求，也不包含凭据或模型配置。未来收费执行还需记录相同模型配置、运行版本和独立预算；如输入或提示词变化，应重新冻结并说明，不能与这里混比。

| 案例 | 方式 | 请求指纹 |
| --- | --- | --- |
| 归属不明 | A | `e88f033ae502e973c98cce43a8b32453fa379ccc3da3c4318afde924eb68dd1d` |
| 归属不明 | B | `ea1abc2169a662d2af064239d40ff00884a391986342559902a19b189e6fde73` |
| 归属不明 | C | `7e378bcfbeaac0ab7815466ec2892954f53f36b561198788ec405137d32be0ed` |
| 上一动作收尾 | A | `8faf934e2bf2eccf0c52e31870e0485c36aae7b37884df300a3f9a2e3081b0e8` |
| 上一动作收尾 | B | `e7ef36b112733c3eb03536d250b476b061fdc98631d2cc3a2257033683ee1051` |
| 上一动作收尾 | C | `52700c90841f847d7f3542f9b1788d657b37929f6a007a3c05d693af58f3bd56` |

包含输入与预期答案的案例指纹：

- `ambiguous_transition`：`e23e6c31126e64f7ea7aed2b95f826ffcac02680c223c252c15fb679712edd41`。
- `previous_action_closing_caption`：`ed5a478e374d9ac41a7e6f8b51fee46f50a9eaae5d23531f711b75ac01f2cfdf`。

## 验证状态

专项首轮 23 项通过，审查修正后为 42 项通过；实验工具连同项目源码与测试的严格类型检查通过。首轮完整 `pnpm check` 为前端 275、后端 1012 项通过；格式审计修正后在 4f2a2d7 完整复跑，前端 275、后端 1031 项通过，lint、类型检查、构建通过。E2E 在 9baa092 为 49 项通过；其后的 4f2a2d7 只修改实验脚本与测试，不改应用代码。

## Standards

独立审查 `c14bc66...9baa092`：硬性规范违反 0，启发式异味 0。增量复核 `9baa092...4f2a2d7`：硬性规范违反 0，启发式异味 0；原响应仅在瞬时校验中读取，新增记录只有格式布尔值；内部客户端关闭不影响调用方客户端。独立专项 42 项通过。当前 Standards 总计 0 项发现。

## Spec

初审发现 1 项 P1：模型返回非法要点选择列表或省略必填字段，生产容错清空/补默认后，实验把解析后结果当作正确模型输出，形成假通过。先在 HTTP 公共边界复现：超限、null、字符串、非字符串元素、缺失要点列表在三种方式下共 15 项失败。随后在实验 HTTP 入口加入原格式审核，只留下布尔值，保留原生产解析和融合流程；另覆盖缺失分组字段和 Ark 两种响应信封。

独立复核 `9baa092...4f2a2d7`：原 P1 关闭，P0 0、P1 0、P2 0；没有给模型追加要点答案，A/B/C 证据、评分标准和生产路径不变。独立专项 42 项通过。当前 Spec 总计 0 项发现。

该修正只保证实验不会把生产容错误报为模型正确，不能证明真实模型已改善。两个审查轴都未执行真实 Provider 调用。

## 尚未执行

六格真实模型对照尚未获新调用授权，也没有执行。实验只支持两条合成视觉观察案例，没有验证 ASR、真实视频、参数冲突或识别稳定性。C 使用正确分组这一额外信息，B 仍保留上游绑定 ID；不能据此直接证明任务负担或锚定偏差是唯一原因。

未改产品链路、正式 Provider、300 秒限制、前端或现有演示入口；不合并、不部署、不开放网络。小米真人语音及完整旅程验收仍待完成。
