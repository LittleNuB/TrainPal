# 连续教学分块试行：验证记录

后续用户另行授权的两次真实复评已完成，结果未通过质量门槛，见 [追加真实复评](trainpal-contiguous-teaching-real-review-20260919.md)。下文保留首次合成验证时的口径，不将其改写为真实通过。

关联 #6；从 #31 的 `6a93e64` 建立隔离分支，功能提交 `6f99ca2`。用户于 2026-09-19 明确允许试行。决策与限制见 ADR-0055，不改变主分支或旧标注工作树。

## 可复现结果

通过已约定的融合公共接口，真实 Ark 客户端配合外部 HTTP 测试替身，使用纯合成文本证据，不调用真实 Provider。

| 输入条件 | 预期与结果 |
|---|---|
| 模型确认同次教学，视觉 0–40 / 30–60 / 50–75 | 修复前返回 3 项的断言失败；修复后 1 项，3 段来源保留，默认仍是原有 0–40，不扩成 0–75 |
| 正反输入顺序、嵌套短片段 | 连通判断和默认来源稳定 |
| 角色未知、跟练角色、编号/左右侧/轮次冲突 | 保留原项、待确认 |
| 视觉间隙或仅端点相接，即使有宽泛旁白 | 保留原项，不用旁白补桥 |
| 中间出现另一个视觉动作或身份未知视觉 | 阻止跨边界合并；范围外视觉不阻止例外 |
| 组内语音时间无关或是跟练、仅语音链、单视觉连接不同语音 | 保留原项 |
| 模型不确定或将变式分为不同组 | 不自动跨组重归类 |
| 同次教学次数 8 / 12 冲突 | 一项待确认，次数留空，两种数值及其各自出处完整保留 |

新增 28 项回归，教学合并、角色保护、无关语音和外部动作边界均先见失败后实现。其他既有保护与新增保留性用例一起复验。提示词同步 2.3.0，不增加模型阶段或调用。

## 检查

- 融合相关四个文件专项：77 项通过。
- `pnpm check`：275 项前端、892 项后端测试通过；lint、仓库配置的类型检查和构建通过。
- 接口生成：无合同内容变化。
- 独立 Standards 审查：0 项；独立 Spec 审查：P0/P1/P2 均 0，固定比较 `6a93e64...6f99ca2`。
- `pnpm test:e2e`：49 项通过，包括响应式、分析到方案、来源存档、跟练与文字片段调整等既有旅程。自动化媒体与麦克风替身不替代真人真机验收。

一次手工类型检查未指定仓库配置时报告 3 个既有模块错误；随后按仓库规定的 `pnpm typecheck` 和完整 `pnpm check` 均通过，未修改无关模块。

## 尚不能声称

这不是修复后真实视频复评，也不是模型准确率或耗时改善证明。旧 12 次真实运行的结论保持原样，未增加收费调用。上游错标教学角色、遗漏动作边界或模型错判身份仍可能误合并；反之未知视觉角色或多余视觉项会让规则继续保守拒绝。

默认回看只选择一个既有片段，不保证覆盖全部教学；播放选项最多 12 项、要点最多三条的旧合同未变。旧方案不自动重写。小米自带浏览器真人语音、其他分析质量问题、逐批合并和干净主分支验收仍未完成。本次不开放网络、不合并、不部署。
