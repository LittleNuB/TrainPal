# 分批交付与继续入口

状态：本轮中间交付，不是整体完成。main 仍为 d21a307；下列均为 Draft，未授权合并，未部署。旧工作树四份未提交标注未修改。

| 批次 | 提交 | 依赖 / PR | Web / API / E2E（各批执行时） |
|---|---|---|---|
| A1 调整 | 59721d7 | main / #19 | 232 / 529 / 26 |
| A2 自动存档 | 1988078 | A1 / #20 | 235 / 529 / 37 |
| B 分析来源 | 826b0ae | A2 / #26 | 237 / 849 / 38 |
| C 前端 | b2ed37c | B / #27 | 243 / 849 / 45 |
| D 低操作跟练 | d7a04de | C / #28 | 258 / 849 / 47 |
| E 文字/语音片段 | 6437237 | D / #29 | 275 / 855 / 49 |
| F 验收材料/本地启动 | 513bf5a（后续文档修订不改业务逻辑） | E / 独立 Draft | 275 / 859 / 49 |

最新 F 完整 `pnpm check`、49 项 E2E、OpenAPI 重新生成无内容漂移、`git diff --check` 均通过。自动化不证明 AI 正确，也不证明小米真人语音可用。320/390/768/1440 视口覆盖来自桌面浏览器模拟。

## 干净环境复现

检出对应 Draft 分支 `agent/release-verification-20260919`，不要复制旧依赖和构建产物。需要 Node 24、pnpm 11、Python 3.12、uv：

```powershell
pnpm install --frozen-lockfile
uv sync --project services/analysis-api --frozen
pnpm check
pnpm test:e2e
pnpm api:generate
git diff --exit-code -- contracts/openapi.json apps/web/src/api/schema.d.ts
```

本地真实使用：将 `.env.example` 复制到被忽略的 `.env.local`，由使用者填入既有 Ark 与 ASR 凭据、已批准模型配置；不把凭据放到前端。`pnpm dev` 开启前端和仅 loopback 的分析服务。本轮开发启动预算见 README；生产入口不采用该开发启动器。真实视频不随仓库分发，只使用使用者有权提供的文件，300 秒上限不变。

按冻结记录顺序从 UI 分别选片运行并对比来源：39/68/100 秒各三次，25/108/208 秒各一次；浏览器真实旅程的一次分析也计入上限。每次记录从点击到可查看方案的耗时、候选/待确认、覆盖缺口及逐项差异。首次执行本轮已经用满 12 次，不应在同一授权预算下自动加跑。历史 pipeline 计时与 UI 计时不可混成同一速度指标。

私密 HTTPS 入口不随启动步骤自动开放；需另外取得网络/访问保护/证书授权。现有本地 15176 预览未替换，不能把它当作这些 Draft 提交的版本证明。

## 下一步顺序

1. 根据 [真实质量报告](trainpal-release-quality-report-20260919.md) 为同示范重复、错绑定和默认完整范围截短建立小型合成回归；在相关分析批次修复，保护编号/变式/再次执行。参数作用范围需补证据，不猜答案。
2. 保持 B 及依赖批次 Draft。修复后重新双轴审查；若需新一轮收费真实复评，单列新预算，不冒充本轮 12 次已验证修复版。
3. 获得临时安全入口授权后，进行 [小米真人验收](trainpal-phone-acceptance-20260919.md)。任何关键语音失败均保留未完成状态。
4. 取得每批合并授权，按 A1→A2→B→C→D→E→F 同步前置基线、重新检查、再合并；任何失败暂停后续。最后从干净 main 复验并据实更新 README。代码合并不包含部署。

## 双轴审查

Standards：A–E 迁移修复后无剩余明确规范问题；F 当前静态复审无明确规范问题，未发现公开材料泄密。

Spec：A–E 代码复审问题已修复，但真实复评另外发现分析质量差距；这些不因静态审查通过而消失。F 不把尚未执行的真机、合并或干净 main 复验写成完成。
