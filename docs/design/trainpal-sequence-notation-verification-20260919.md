# 编号格式兼容：验证记录

关联 #6，基线 #33 / `a294f0f11096656551726ac929bc9b23b6079240`，功能提交 `de7171e`。在独立工作树中修复，保留其他工作树及原演示服务。

## 复现与最小修复

公共融合接口输入两个纯合成视觉观察：编号分别为 `动作4` 和 `第四个动作`，来源范围 0–20 / 10–30 秒，模型返回同次示范。真实 Ark 客户端使用外部 HTTP 测试替身，不调用收费 Provider。

修复前运行 `uv run --project services/analysis-api pytest services/analysis-api/tests/test_semantic_sequence_notation.py`：`assert 2 == 1` 失败，诊断为 `group_rejected=1` 与 `sequence_label_conflict=1`。两个观察、两个等价标签、一个重叠区间足以稳定复现；没有引入视频、ASR 或前端变量。

按 ADR-0056 只增加完整 `第N个动作` / `第N动作` 的身份比较语法，同一测试通过。之后逐项验证保留性边界：不同数字、侧别、轮次、变式、未知格式、再次执行、模型分组与不确定性。正式合并和参考关联仍保留原输入、来源和参数冲突，不拼接播放范围；视觉单源仍待确认。

## 检查

- 新增 29 项回归全部通过；六个融合测试文件共 135 项通过。
- `pnpm check`：前端 275 项、后端 950 项通过；lint、69 个 Python 文件的严格类型检查、前端类型检查与生产构建通过。
- `pnpm test:e2e`：49 项通过；`pnpm api:generate` 后接口合同和前端生成类型无内容变化。
- 独立 Standards：明确违规 0、Fowler 启发式问题 0。
- 独立 Spec：P0/P1/P2 均 0。
- 双轴审查范围 `a294f0f...de7171e`；审查后只增加本记录，不修改运行实现。

## 尚未证明

这不是 eval-08 的真实修复后复评。先前已知三个正式分组因编号冲突拒绝，但具体标签未保存，不能断言本次语法扩展命中原问题。视觉跨块错标、错误分组等原因仍需另证；不宣称准确率或候选数量改善。

本批没有修改视觉/融合提示词、Provider、诊断日志或接口，也没有新增收费调用、开放网络、合并或部署。AI 质量、小米自带浏览器真人语音以及干净主分支最终验收仍未通过。E2E 合成媒体和移动视口不能替代上述门槛。
