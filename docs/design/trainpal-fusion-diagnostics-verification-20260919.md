# 融合拒绝原因诊断：验证记录

后续用户单独授权的一次真实诊断已完成，见 [真实复评记录](trainpal-fusion-diagnostics-real-review-20260919.md)。本次三个正式分组均被编号冲突拒绝，真实质量仍未通过；下文保留代码交付时的验证口径。

关联 #6；基线为 #32 的 `29fc835e4398de7385e88f00862e0a8d256dcd91`，功能提交 `b756a36`。本批只定位后续运行的拒绝原因，不修改合并条件、Provider、提示词或用户确认策略。主分支和旧标注工作树未改动。

## 行为与隐私

新增 29 项回归测试，通过既有融合公共接口、真实 Ark 客户端及外部 HTTP 测试替身验证：

- 正式分组的编号、角色、时间和模型不确定原因可以同时计数；每组同一原因只计一次。
- 保留共同交集与连续教学例外；视觉间隙、外部动作边界和无关语音继续阻止不安全合并。
- 正式分组的参数冲突与参考关联失败分开统计；失败、超时和取消保持原行为。
- 安全日志只接受固定原因码及 0–200 的整数，排除任意文本、布尔值、非法数值及内容字段。
- 诊断不进入 HTTP/SSE、归档方案或永久用户存储。

另以固定种子 20260919 生成 1,000 组纯合成输入，对比基线与新实现的完整候选和警告，结果全部一致。此离线对比使用本地临时检查脚本，不调用 Provider，不包含真实视频信息；正式可复现回归位于 `test_semantic_diagnostics.py`。

## 自动化与审查

- 融合专项五个测试文件：106 项通过。
- `pnpm test:e2e`：49 项通过。合成媒体、移动视口与麦克风替身不等于小米真机真人语音验收。
- `pnpm api:generate`：接口合同及前端生成类型无内容变化。
- `pnpm check`：275 项前端、921 项后端测试通过，lint、仓库配置的类型检查及生产构建通过。
- 初次完整检查发现两处导入排序及两个新测试列表的类型推断问题；已排序并补充类型注解，不修改运行行为。
- 独立 Standards 审查：明确违规 0、Fowler 启发式问题 0。
- 独立 Spec 审查：P0/P1/P2 均 0。

双轴审查固定范围为 `29fc835...b756a36`；审查后仅调整工具要求的导入排序、测试类型注解及本验证记录。

## 仍未通过的门槛

本批没有新增收费调用。之前 208 秒运行没有保存原始分组，不能事后还原具体拒绝原因；需要另行授权一次真实运行，才能获得新诊断。本批合成测试不能证明真实去重质量改善，之前真实复评未通过的结论保持不变。

AI 质量复评、小米 15 自带浏览器真人语音、逐批合并及干净主分支完整旅程仍未完成。不开放网络，不合并，不部署，不把本批诊断代码描述为产品整体完成。
