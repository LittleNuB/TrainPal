# 分钟单位修复：GAP-01 首批

来源：当前用户同意继续修复，以及本机差距审查中的 GAP-01。历史 eval-07 候选把 20 分钟爬坡有氧写成 20 秒。本批不修改 Provider 选型、运行架构、产品时长上限或训练执行规则。

## 范围与验收

通过 `ArkResponsesClient.understand_speech` 公共接口处理结构化响应，再经真实 `fuse_candidates` 输出候选；只有外部 HTTP 返回在测试中替代，内部处理不模拟。

- 时间戳位于对应 signal 内的 ASR 语句明确写出与动作名直接相邻的单个阿拉伯数字分钟值，且模型把原数字错误当秒数时，进行一次 ×60 换算；20 分钟对应 1200 秒。
- 已是 1200 秒的不重复换算；20 秒保持 20 秒；缺失值不自动补全。
- 不读取模型生成的 evidence_text 推断单位，不读取全片无时间的 text 兜底；不借用相邻动作的数值或范围之外的语句。
- 范围、近似、否定、复合时长、休息语境、延迟开始与视频长度不在此窄规则支持范围内，原结果不由该规则修改。这不代表这些原结果已被验证正确。
- 仅处理单一 duration 字段且没有 reps/rest 的情况；不处理休息换算、中文数字、动作别名或新的参数抽取任务。保守不匹配仍交由现有流程；不能宣称解决所有单位问题。
- 审查后进一步收紧为完整短句白名单：可选“训练后/练后、可/可以、再、加/做/进行”前缀，或已观察到的精确 ASR 前缀“晚可以加”，随后“数字＋分钟＋完整动作名”，可带句末标点。动作在时间前、复杂长句、额外限定词不自动修正。这样不靠不断扩大黑名单猜测自然语言；代价是覆盖较窄，不是通用口语单位解析。

## 证据与测试过程

历史原始响应已按隐私约定清理。测试使用同义的脱敏语句与构造的错误模型返回，不伪称历史原始响应回放。

首个接口测试实际失败 `20 != 1200`；加入边界修正后通过。随后复合时间、延迟开始、视频时长、相邻动作保护测试暴露误修正，逐轮收紧规则后通过。

该测试证明当前接口能防住这个受限输入模式，不证明此前历史请求的唯一根因就是模型换算错误。重复候选和动作错位未在本提交修复，不以模糊合并换取候选数量下降。

命令：`uv run --project services/analysis-api pytest services/analysis-api/tests/test_speech_time_units.py -q`。

## 真实语音分支复测（2026-09-10）

在用户已授权的服务和视频范围内，仅对 eval-07 全长 67.83 秒音轨复测，未调用视觉模型、未运行整条产品链路。

1. 首次复测：模型原始 duration_seconds=20，规则输出仍为 20，暴露实际句式不匹配。
2. 补核短证据：60.58–62.06 秒 ASR 为“晚可以加20分钟爬坡有氧。”。仅保留这段匿名短证据，不据此纠正其他语义或丢弃原片条件。
3. 将实际句式及其“最多”“不可以”保护用例加入接口测试，先复现失败 20 != 1200，再增加精确前缀支持；84 个定向测试通过。
4. 再次真实复测：动作爬坡有氧，60.58–62.06 秒；模型原始值仍为 20，公共 Provider 接口输出为 1200，确认此次是规则纠正而非模型恰好答对。

本轮共执行 3 次 ASR、2 次语音理解请求（包含一次仅补核 ASR）；未记录费用金额。所有临时音轨均随临时目录退出清理，未保存完整转写或原始模型响应。原始视频不删除。

历史开发分支回归（2026-09-10，90f35eb）：前端 223 项、后端 529 项（含 84 个单位保护接口用例）、端到端 38 项通过；lint、类型检查及构建通过。当时 Standards 和 Spec 双轴增量复审各有 0 项未关闭发现，比较基线为 cac744b。这些数字不代表从主分支独立移植后的验证。

此结果不能外推为 12 条视频全部通过、90% 准确率或分析性能达标。截至上述历史验证，仅本地提交、未推送或部署；既有标注工作文件保留。

## 独立 PR 基线（2026-09-11）

关联 [Issue #4](https://github.com/LittleNuB/TrainPal/issues/4)，仅交付分钟单位修复首批，不关闭整个基线整理任务。

- 基于个人仓库 main `7b238ef715a311fd1fa7c9baa6ea1ae227d7addf` 新建隔离分支，仅移植分钟修复的四个文件，不纳入其他本地历史提交、前端重构或未提交标注材料。
- 新基线 Spec 审查发现此前遗漏：模型把“休息”误放到 duration 字段时，仍会触发转换。公共接口先复现 `1200 != 20`，再显式排除休息、间歇、组间及 rest 语境。该规则不处理休息换算，也不证明未转换原值正确。
- 新增动作名参数化测试，保护“做20分钟休息”“做20分钟组间间歇”“做20分钟Rest”；原有真实短句保护仍保留。
- 未进行新的付费 Provider 调用。上一节的真实语音复测属于旧开发基线，不冒称本 PR 完整真实链路复测。
- 本 PR 的 E2E 首轮 21 通过、1 失败；单独重跑和未修改的 main 同一用例均复现导航小字对比度 2.38（测试要求 4.5）。这是基线门禁问题，归 #4 后续独立修复，不混入分钟规则改动。
- 主分支镜像工作流仍有历史队友镜像目标，特性分支推送和 PR 只运行 CI，不触发该发布工作流。合并前必须单独处理发布边界；本 PR 保持 Draft，不执行镜像发布或部署。

最终新基线检查结果与线上 CI 状态以本 PR 说明为准，不沿用历史分支通过数。

## 主分支整合后的复测（2026-09-11）

- 上节的导航失败与旧镜像工作流状态是首次建 PR 时的历史记录。主分支现已通过 #13–#15 修复导航对比度、发布隔离和取消清理；本分支以 merge 同步 `d0d6a49d953ce25d7daa152e4a026d54710d3abb`，没有重写已有提交。
- 相对新 main 仍只有原定四个文件；分钟处理逻辑与接口测试未因同步而改变。
- 本地 `pnpm check` 通过：前端 204、后端 526；96 项分钟保护接口用例通过，OpenAPI 无内容漂移。
- 默认并行 E2E 首轮为 21 通过、1 失败。截图显示分析名额占用；缩小为受控来源与本地上传两项旅程各重复四次，8 workers 下本分支 3 失败、未修改 main 5 失败。二者共享后端单分析槽，属于需要独立处理的测试调度问题，不能归因于分钟修复或通过放宽产品并发消除。
- 同一分析旅程单 worker 连续三次通过；完整 E2E 显式 `--workers=1` 为 22 通过。默认 worker 配置不在本 PR 修改，单独提交测试隔离修复；不能把串行通过写成原默认并行模式已修复。
- 本轮没有真实 Provider 调用、媒体上传、合并或部署。GitHub CI 的新结果以 PR 检查区为准；PR 继续保持 Draft。

## 测试隔离与超时清理整合后的合并前检查（2026-09-11）

- 用户本次确认继续合并已验证修复；#18 先进入 #16，组合 CI 通过后，#16 已合入 main `dc7356772427c7feb90164d4e55373e8fe39f0a2`。这替代上一节尚待处理 #16 / #17 的状态，不删除历史失败记录。
- 本分支通过 merge `0df8e4504c3ede56b6b96f76e4e3576e8a968bd2` 同步新 main，无冲突；相对新 main 仍仅四个原定文件，分钟实现、Provider 接入与 96 项接口测试相对 `5894710` 无变化。
- 默认 E2E 现在继承 #16 的单 worker 配置，无须命令行临时覆盖；超时清理修复来自 #18，不属于本 PR 的分钟规则改动。
- 本次完整本地检查、默认 E2E、新基线双轴复核和 Linux CI 的最终回执记录在 PR #12；未完成前不合并，不沿用上一节的通过数。
- 本次无新的真实 Provider 调用或媒体上传；仓库合并不等于部署，不运行镜像工作流，不操作云资源，旧树未提交标定文件保持原样。
