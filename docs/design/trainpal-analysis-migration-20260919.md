# B 批：分析与来源迁移记录

固定基线：#20 的 1988078；迁移参考只读集成树 3c2acd6。关联 #5/#6/#7/#23，不关闭问题。没有迁入 DeepSeek 实验、原始媒体、完整转写、密钥或历史评测结论。

## 范围

迁入时间与编号约束下的模型语义分组、中文名称与可见训练文字、分钟单位核验、预告/回顾的仅来源关联、同动作参数冲突及最多三条视频要点。同步 OpenAPI/前端类型；接受、编辑、自动归档、打开与刷新保留来源资料和用户值。方案编辑器显示要点、出处及冲突，训练中展示留给 D。

只在既有 Ark 文本接口增加已批准的语义处理，不改变视觉/ASR Provider、不引入 Agent 循环、不改变训练量或 300 秒上限。0049/0050 已用于独立方案库决策，旧语义 ADR-0050 迁为 0053；单动作字段冲突另记 0054。

## 测试与审查

- 缺少语义模块、来源字段未保存、要点组件缺失均先建立失败测试，再迁移实现。
- 新 E2E 验证一条受控结果经修改次数、确认、存档、重新打开和刷新后，用户值、要点和参数冲突仍保留。首次运行缺合成视频；补齐素材后又修正测试未接受浏览器确认框的问题，最终通过。这两次为测试准备/脚本问题，不是模型质量证据。
- 首轮完整检查：237 前端、844 后端、38 E2E 通过。
- Standards 初审 2 项 P1，Spec 初审 1 项 P1（与前者重合）：实际提示词仍要求参数冲突拆项；语音失败被过滤前非训练画面掩盖。分别新增实际 Skill 加载合同与公共 pipeline 四组合测试，先红后绿。
- 修复后提示词 2.2.0；同次身份与参数冲突解耦；过滤后判空，语音失败无候选返回可重试缺口，正常无训练内容返回 no_evidence。
- 修复后 `pnpm check`：237 前端、849 后端，Lint、类型、构建全部通过。Standards 与 Spec 复审均 0 未关闭发现。

## 未验收项

本轮尚未运行真实视频复评，39 秒四动作全部待确认的原因尚未在此基线重测；不宣布准确率/去重质量达标。小米真机、真人语音、训练时提示与新执行流程未在本批验收。未切换当前演示服务、未合并或部署。
