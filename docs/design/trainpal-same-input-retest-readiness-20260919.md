# 同输入复测：准备验证记录

基线 #34 / `c0102bc`；工具提交 `da97a7d`、`a6320c3`。这是一份复测准备记录，不是新的真实视频质量报告。

后续状态：用户已单独授权最多四次条件式复测；实际运行 eval-08 一次。去重改善，但提醒来源错配导致第一道门槛未通过，后三次停止。见 [真实复测报告](trainpal-same-input-real-review-20260919.md)。下文保留授权前的准备状态，不作为当前运行结论。

## 已准备

- 本地工具位于 `services/analysis-api/scripts/fusion_comparison.py`，产品代码没有导入。它包装已批准的融合入口，实际模型只提供一份分组；离线回放核对相同输入，不发起第二次 Provider 请求。
- 旧规则是 a294f0f 的编号解析策略，其余融合代码与当前版本相同。隔离模块避免污染当前规则；明确不称为完整旧构建 A/B。
- 固定分类区分格式等价、不同可解析数字和包含未知格式的身份冲突，不保留标签/编号/分组成员。分类不是对视频真值或模型错标的判决。
- 私有启动器已准备好一次性运行登记和 eval-08 人工复核门槛；后续样本必须等该门槛标为通过才能运行。真实媒体路径仅在本机忽略目录，公开代码没有媒体映射或凭据。
- 只读预检确认四条已授权视频可访问且均小于 300 秒，均未开始本轮收费分析；未加载凭据或启动 Provider。

## 验证

- 先写“同输入比较产生 2→1 且只请求一次模型”的失败测试，再实现工具；最后新增 13 项回归全部通过。
- 覆盖格式修复、原本等价、不同数字、未知修饰、多原因共存、取消、超时、Provider 错误、无效成员、零预算、拒绝重复运行、取消后恢复原入口和 200 条输入的计数边界。
- `pnpm check`：前端 275、后端 963 项通过；lint、类型检查、生产构建通过。单独将复评脚本纳入严格类型检查，71 个文件通过。
- `pnpm test:e2e`：49 项通过。接口重新生成无合同内容变化。
- 独立 Standards：明确违规 0、Fowler 启发式问题 0；独立 Spec：P0/P1/P2 均 0。固定范围 `c0102bc...a6320c3`；之后仅增加本记录。

首次单独类型检查未带 src 范围，出现包类型发现错误；加入完整仓库范围后发现并修正脚本的模块属性写法及可选值类型。另一次 lint 提醒动态属性写法，改为隔离模块字典赋值后完整检查通过，没有关闭规则或引入忽略注释。

## 待执行

最多四次收费运行的独立授权尚待确认：208 秒一次，通过后才各跑 68、100、39 秒一次，失败即停止、不自动重试。具体通过标准见 [复测规格](trainpal-same-input-retest-spec-20260919.md)。

原始真实质量失败结论保持。本轮没有新增收费调用、修改产品规则、合并、部署或开放网络；真机真人语音与干净主分支验收仍未完成。
