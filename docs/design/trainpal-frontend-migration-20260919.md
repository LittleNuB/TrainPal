# C 批：已确认前端方向迁移

## 范围与依赖

依赖 B 批 826b0ae 和 A 批 #19/#20。迁入已确认 C 方向：暖白、浅绿、陶土色主操作，手机单列与桌面双列，分步调整，以及可拖动、键盘移动、折叠和位置记忆的小猫。关联 #8。

没有迁入低操作训练状态机、语音接口、Provider 切换或新的视觉方案。旧训练流程只换视觉。自动存档和调整回滚继续使用 A 的实现；来源要点与冲突沿用 B。上传大小取服务端能力并受可选构建上限约束；生产构建仍限制 19 MB，时长仍为 300 秒。

## 回归保护

- TrainHub 等待方案写入时继续禁止离页，成功后才解锁编辑。
- 复练前等待草稿写完，保持自动存档顺序。
- 调整方向改变后丢弃旧建议；应用/恢复进行中保留重复操作保护。
- 慢保存测试先复现缺失按钮禁用，再补回保护；原有存储失败、复制、删除冲突与恢复基础方案测试保留。
- 旧入口文案对应的新测试仍执行相同行为，未移除原有断言来跳过失败。

## 视觉核对

对照用户确认的 C 方向参考图及本批实际 390 / 1440 截图，检查：

1. 暖白底、浅绿摘要与陶土主按钮一致。
2. 手机保持单主列，桌面方案概览和动作清单分区。
3. 大标题、摘要、动作名称与次级来源说明有层级。
4. 动作编辑、训练调整进入独立面板，主列表不堆叠全部字段。
5. 固定底部操作有内容留白，小猫避让关键按钮；四种视口无横向溢出。

参考图不是像素级验收图。保留已确认运行版的编号动作列表和七套教练素材；“另存为”改为 A 批自动存档后的“复制方案”。这些是已有合同差异，不声称与生成图完全一致。

本地视觉证据在忽略目录 `tmp/audit/redesign/`，不提交用户媒体或浏览器数据。预览使用独立本机端口，未替换用户正在使用的版本，也未开放局域网入口。

## 验收边界

2026-09-19：完整检查通过（242 项前端、849 项后端），45 项端到端通过。独立 Standards / Spec 各发现同一项 P2：训练页在保存失败时仍宣称已保存。已移除该无条件成功声明，新增失败进入训练页回归测试，先失败后修复。慢保存应用与恢复保护也经过先失败后修复的测试。

自动化结果在 PR 更新；本批不代表真实 AI 质量、真机解码、锁屏或真人语音通过。小米自带浏览器的 10–15 分钟验收仍为 F 批阻断门槛。主分支、README 和部署未改动。
